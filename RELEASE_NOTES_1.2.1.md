# CriticFlow v1.2.1

> GitHub v1.2.1 为 **prerelease**。已构建 Obsidian 发布文件，但未运行测试或 GUI 验收；实现与部署不等于实机通过。

## Obsidian 阅读模式定位

- 定位策略由“全页 48 字强制前后文校验”调整为绑定源 Markdown section 范围与文件快照。
- 对重复出现的文本按出现次数与位置定位，不仅依赖文本内容猜测。
- 格式差异与软换行差异通过保留 offset 的 prose 映射定位。
- 新增批注保留原 Markdown 的 raw slice，不再用 DOM 渲染文字覆盖源码片段。
- 支持 HTML 实体的可见字符映射；跨粗体、斜体或链接边界时扩展到完整格式片段，避免把配对标记拆开。此时高亮范围可能比划选略大，但原文不会被重写。
- 跨块高亮仍保留 CriticMarkup 源码；跨独立渲染 section 或 Markdown 块的渲染限制仍在。
- 本版本不再以“推荐切换编辑模式”作为规避新增定位错误的说明。

## MarkEdit

- MarkEdit 脚本逻辑与 v1.2.0 相同。
- 本机安装流程已完成机械核对：先 rename 旧文件备份，再覆盖目标文件。
- 不声称脚本已在 MarkEdit 界面加载，也不声称界面运行通过。

## 发布边界

- 本次实际修改 Obsidian 定位实现并重构建发布文件，不只是版本号或文档更新。
- 未运行测试、未进行 GUI 验收；构建成功不等同于宿主实机验收。
