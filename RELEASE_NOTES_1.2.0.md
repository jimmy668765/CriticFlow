# CriticFlow v1.2.0

> GitHub v1.2.0 为 **prerelease**。核心实现已完成，但尚未完成全端实机验收；本版本不宣称稳定。

## 本版本已实施

### Obsidian

- 批注目标会冻结原始 `TFile`、路径、编辑器身份与全文快照。
- 新增与编辑均按精确选区及批注 offset 替换，避免仅凭重复文本猜测位置。
- Reading View 保存前会通过 `Vault.process` 检查快照；检查失败时保留弹窗并明确报错，不提示成功。
- Reading renderer 按 section 范围映射源批注；范围有歧义时拒绝猜测。Live Preview 使用 `StateField` 支持多行折叠。
- 多行折叠的实现路径已完成，但尚未实机确认所有 Reading View 渲染场景。跨独立渲染 section 的长备注仍可能显示源码，建议此类批注在编辑模式处理。

### MarkEdit

- 批注交互绑定 `EditorView`、文档快照与 offset；文档发生变化时不会把批注写入错误位置。
- 保存前后都会检查文档状态；失败时保留弹窗并明确提示。
- `saveDocument(): Promise<boolean>` 可用时支持自动保存；API 缺失或保存失败会明确提示使用 `Cmd+S`。
- 保存重试只重新保存，不会二次插入批注。API 能力已对照官方源码及实际安装 bundle 核对，但尚未完成实机验收。

### Paseo

- 批注绑定当前工作区、原始文件与选区位置，支持重复文本区分及 CRUD。
- 保存流程为备份、最后检查、写入临时文件并原子 rename；这不是跨进程原子 CAS。外部编辑器在极短窗口内仍可能产生冲突，请避免多个编辑器同时写入同一文件。
- 支持同一 Markdown 块内跨文本节点的批注显示。跨段落/列表项等块级边界暂时保留源码，不执行可能破坏块结构的 Range 删除；此项显示支持仍未完成。
- Paseo 原生手机端不支持。

## 发布边界

- 本次文档描述的是已实施代码，不等同于实机验收报告。
- Obsidian 最低版本为 1.1.0。Paseo 和 Obsidian 阅读模式对跨块批注采取保守显示策略，可能没有高亮或气泡；源文件批注不因此丢失。
- 未运行测试，也未完成 GUI 或各宿主的完整实机验收。
- Obsidian 根目录 `main.js` 与 `styles.css` 已由实际构建同步。
