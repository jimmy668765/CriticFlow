# v1.2.1 源码修复与本机部署记录

## 触发问题与代码原因

用户在 Obsidian 阅读模式收到“选区与源文件无法唯一对应，未写入；请在编辑模式批注”。v1.2.0 的定位实现要求渲染页面前后各 48 字与源文件完全吻合，即使原文只有一处匹配也不能例外。标题、属性栏、虚拟化邻接内容、Markdown 格式与软换行都可能让这一条件失效。截图本身没有选区原文，不能声称已复现该次具体操作。

## 实际修改

- `document-target.ts`：绑定 postprocessor 的 sourcePath、section 范围与快照；上下文不再作为唯一候选的硬门槛。重复项在来源范围、上下文或源/DOM 出现次数一致时消歧，不取第一处。排除嵌入笔记、标题/属性与现有批注内部的新增选区。
- `source-projection.ts`：保留源码 offset 的可见文字定位，处理常见 Markdown 链接、格式和 HTML 实体。格式化与非格式化的重复项一起计数，防止唯一字面匹配错误胜出。
- 新增批注通过回调包裹原始 Markdown 切片，不再用 DOM 文案替换原文。跨配对格式边界会扩展至完整格式片段，范围可能比划选略大。此定位辅助不是完整 Markdown 解析器，不声称覆盖所有语法组合。
- `main.ts`：即使关闭折叠也登记阅读 section；保留文件快照比较与原文件 `Vault.process` 写入。

## 复核与构建边界

进行了只读独立源码复核，发现并修正配对格式边界与 HTML 实体两项问题。最终 Obsidian production bundle 构建成功，根发布资产与本机 canonical 插件三个运行资产逐字节一致。依用户要求，没有运行测试或操作 GUI；源码复核和构建都不是实机验收。

## 本机覆盖

- Obsidian：1.2.1 的 `main.js`、`manifest.json`、`styles.css` 已备份并覆盖 canonical 安装，需宿主重新加载。继续保留此前重复安装清理结果。
- MarkEdit：两份旧脚本通过 rename 完整移入工作台外的私人备份目录，随后分别以独占创建写入新版脚本；两次 write、flush、fsync 均完成，文件大小均为 30789 字节。脚本实现与 1.2.0 相同。
- MarkEdit 目标文件回读仍超时，不能声称完成内容哈希复核；但本次不再是“未覆盖”。没有操作宿主 GUI，不能声称新脚本已加载。

## 仍然存在的边界

Paseo/Obsidian 阅读模式跨 Markdown 块的高亮仍保留源码；Paseo 写入不是跨进程 CAS；移动端与三端真实交互仍未验收。避免其他编辑器同时写入同一文件。
